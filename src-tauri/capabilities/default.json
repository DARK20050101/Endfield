{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local",
  "description": "Local probe only; no remote-page IPC, file access or frontend HTTP.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
